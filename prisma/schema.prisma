generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum ArticleStatus {
  COLLECTED
  PROCESSING
  READY_TO_PUBLISH
  PUBLISHED
  FAILED
}

model Source {
  id        Int       @id @default(autoincrement())
  name      String
  type      String
  url       String
  active    Boolean   @default(true)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  articles  Article[]
}

model Article {
  id          Int      @id @default(autoincrement())
  externalId  String
  title       String
  content     String
  url         String
  status ArticleStatus @default(COLLECTED)
  collectedAt DateTime @default(now())
  processedAt DateTime?
  publishedAt DateTime?

  sourceId Int
  source   Source @relation(fields: [sourceId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}